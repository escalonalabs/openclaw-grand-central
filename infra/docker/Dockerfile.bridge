FROM node:20-alpine

WORKDIR /workspace
ENV NODE_ENV=development

EXPOSE 3000

CMD ["node", "-e", "require('http').createServer((_,res)=>res.end('bridge dev service')).listen(process.env.BRIDGE_PORT || 3000, '0.0.0.0')"]
