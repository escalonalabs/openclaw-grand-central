{
  "name": "@openclaw/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw Grand Central schema package",
  "scripts": {
    "test": "node --test ./test/*.test.ts"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  }
}
